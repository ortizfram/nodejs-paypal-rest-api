<!-- forgotPassword.ejs -->
<div id="forgot-password-container">
  <h1 className="section-title"><%= titles %></h1>
  <% if (subtitle) { %>
  <p className="subtitle"><%= subtitle %></p>
  <% } %>
  <form method="POST" action="<%= formAction %>" enctype="multipart/form-data">
    <% for(let i=0; i < fields.length; i++) { %> <% if (fields[i] !=='avatar' )
    { %><!--avatar is for userUpdate template-->
    <label for="<%= fields[i] %>" className="label"> <%= labels[i] %> </label>
    <input
      type="text"
      id="<%= fields[i] %>"
      name="<%= fields[i] %>"
      placeholder="<%= fields[i] %>"
      value="<%= values[i] %>"
    /><br />
    <% } %> <% } %> <% if (fields.includes('avatar')) { %>
    <!-- Check if avatar field exists -->
    <label for="avatar" className="label-avatar">Avatar:</label><br />
    <div className="avatar-uploader">
      <img src="<%=user.avatar%>" className="avatar-profile" alt="user-avatar" />
      <input type="file" id="avatar" name="avatar" /><br /><br />
    </div>
    <% } %>
    <button type="submit"><%= submitBtn %></button>
  </form>
  <% if (message) { %>
  <p><%= message %></p>
  <% } %>
</div>

<style>
  #forgot-password-container{
    margin: 6.2rem auto;
  }
</style>
